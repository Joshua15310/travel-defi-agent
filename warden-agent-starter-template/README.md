# Warden Agent Starter Template 🚀

This is a clean, minimal Python template for building a new Warden Protocol AI Agent based on the LangGraph framework. It provides all the necessary scaffolding to jump straight into writing your business logic.

## 📦 What's Included

* **Core Structure:** A pre-configured `agent.py` file with the basic LangGraph node structure.
* **Dependencies:** A starter `requirements.txt` that includes `google-genai` and core LangChain packages.
* **CI/CD Ready:** Includes a basic `.github/workflows/python-tests.yml` file to ensure continuous integration.
* **Environment Setup:** A `.env.example` file clearly defining required API keys (Gemini, Warden, etc.).

## ➡️ Quick Start

1.  Clone this repository.
2.  Install dependencies: `pip install -r requirements.txt`.
3.  Fill in the required environment variables.
4.  Begin writing your custom tools and logic in the `tools.py` file!